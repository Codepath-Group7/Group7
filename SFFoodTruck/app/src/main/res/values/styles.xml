<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>
    <style name="AppTheme.DetailView">
        <item name="colorPrimaryDark">@color/colorPrimary_Alpha</item>
    </style>

    <style name="RatingBarStyle" parent="Widget.AppCompat.RatingBar.Small">
        <item name="android:minHeight">@dimen/ratingBarMinHeight</item>
        <item name="android:maxHeight">@dimen/ratingBarMaxHeight</item>
    </style>

    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
    </style>


    <style name="AppBarTheme" parent="AppTheme">
        <item name="colorPrimaryDark">@color/colorPrimary_Alpha</item>
    </style>

    <dimen name="ratingBarMinHeight">20dp</dimen>
    <dimen name="ratingBarMaxHeight">24dp</dimen>
    <color name="white">#ffffff</color>

    <color name="white._a">#64ffffff</color>

</resources>
